@model MvcBookshelf.Models.StatisticsViewModels.StatisticsViewModel // similar pathing as Razor instead of Enumerable
@{
    ViewData["Title"] = "Your Bookshelf Statistics";
}

<h1>Your Bookshelf Statistics</h1>
<p>View a summary of how many books, authors, genres, and total pages from your bookshelf.</p>
<table>
    <tr>
        <th>
            Completed Books
        </th>
        <th>
            Authors Read
        </th>
        <th>
            Genres Read
        </th>
        <th>
            Pages Read
        </th>
    </tr>
    <!-- use at-symbol model like Index.cshtml to access the page model for StatisticsViewModel numbers -->

    <tr>
        <td>
            @Model.TotalBooks
        </td>
        <td>
            @Model.TotalAuthors
        </td>
        <td>
            @Model.TotalGenres
        </td>
        <td>
            @Model.TotalPages
        </td>
    </tr>
</table>